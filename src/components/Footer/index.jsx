import React from 'react'

import { ContainerFooter} from './styles'

const FooterContainer = ({title}) => {
  return (
    <ContainerFooter>{title}</ContainerFooter>
  )
}

export { FooterContainer };
